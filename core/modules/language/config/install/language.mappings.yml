# Browsers use different language codes to refer to the same languages,
# these defaults handles the most common cases.
map:
  no: 'nb' # Norwegian
  pt: 'pt-pt' # Portuguese
  zh: 'zh-cn' # Default Chinese to simplified script
